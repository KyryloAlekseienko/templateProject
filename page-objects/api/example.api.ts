export class ExampleApi {
  public async deleteById(id: number): Promise<void> {
    // console.log(`Deletingi ID=${id}`);
    // const response = await ApiClient.delete<{
    //     statusCode: number;
    //     message: string;
    //     isError: boolean;
    // }>(
    //     `${API_ENDPOINT.....}/${id}`,
    // );
  }
}
